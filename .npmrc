# Internal packages are consumed as TypeScript source (see docs/03-ARCHITECTURE.md §10).
# A strict store keeps phantom dependencies from compiling by accident.
strict-peer-dependencies=false
auto-install-peers=true
