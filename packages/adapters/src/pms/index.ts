// Shared PmsAdapter contract-test suite and fixtures.
//
// Every implementation of `PmsAdapter` (mock today, Ericsoft when WS-C clears)
// runs against this same suite. Passing it is the precondition for swapping the
// mock out (ADR-008).
//
// Fills in day-1 task 4.
export {}
