// The shared `AlloggiatiAdapter` contract suite, and the future home of the
// real channel (E2.3).
//
// A real implementation — direct web service or certified intermediary,
// whichever 04 §0 item 5 lands on — must pass `describeAlloggiatiContract`
// before it replaces the mock.
export * from './contract'
