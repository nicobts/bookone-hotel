# Golden eval sets

One directory per agent. Every agent has an eval set **before** it has
production traffic (06-AI-AGENT-LAYER §1.5); the set grows from reviewed
production failures. `pnpm test:evals` is one of the five CI merge gates.
